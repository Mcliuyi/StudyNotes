# Volatile

## 实现原理

利用利用操作系统内存屏障将指令分割，同时禁止编译时期指令重排序的优化操作，保证一致性，当某个线程将volatile变量修改完毕后，会立即调用操作系统指令将修改后的值重新刷入内存

## 特性

### 可见性

对一个voltaile变量的读，总是能看到(任意线程)对这个volatile变量最后的写入

### 原子性

对任意单个volatile变量的读/写具有原子性

### 有序性

禁止指令重排序，保证有序性